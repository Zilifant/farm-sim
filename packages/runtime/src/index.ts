// @sim/runtime — reusable simulation infrastructure.
// Subsystems land here in later phases: clock, schedule, workers, memory,
// messaging, rng, events, snapshot, profile.

export const RUNTIME_NAME = "@sim/runtime";
